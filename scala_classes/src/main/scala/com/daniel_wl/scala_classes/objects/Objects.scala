package com.daniel_wl.scala_classes.objects

//objects are what would be called static classes in other languages
object Objects {
  //all methods on objects are what would be called static methods
  def staticMethod: String = {
    "static method return value"
  }
}
